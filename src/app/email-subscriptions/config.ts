import { FormLayout } from '@/components/library';

export const formFields: FormLayout = [
	{
		sectionTitle: 'Subscription Details',
		fields: ['email', 'isActive'],
	},
];

export const tableFields = ['email', 'isActive', 'createdAt'];
export const fields = ['email', 'isActive', 'createdAt'];
