import { FormLayout } from '@/components/library';

export const formFields: FormLayout = [
	{
		sectionTitle: 'Banner Details',
		fields: ['image', 'name', 'priority'],
	},
];

export const tableFields = ['name', 'priority', 'createdAt'];
export const fields = ['name', 'image', 'priority', 'createdAt'];
